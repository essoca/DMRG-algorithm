function Install()
% On the Matlab prompt, change into the D-MiRaGe directory and run Install.
  dir = pwd;
  addpath(genpath(dir));
  disp(sprintf('\n%s directory added to Matlab search path.\n',dir));
  
